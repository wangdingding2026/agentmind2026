# AgentMind 记忆和检索模块优化方案

> **版本**：v4（最终方案）
> **创建日期**:2026-05-25
> **基础版本**：AgentMind v3.0
> **目标定位**：完整的 Agent 记忆引擎（非单纯对话历史检索）

---

## 背景

AgentMind v3.0 当前记忆引擎基于 **SQLite + FTS5 + sqlite-vec**，已实现基础写入/混合检索（RRF 融合）/热度统计/语义冲突检测。面对未来长期记忆量增长（百万级）+ 精准检索要求，存在以下短板：

- sqlite-vec 线性扫描，10 万级以上检索性能急剧下降
- FTS5 默认 `unicode61` tokenizer 对中文检索精度不足
- embedding 维度硬编码，切换模型必须全量重建
- 无 importance 评分、无 dedup、无蒸馏、无关系图、无 Core Memory
- 写入侧逻辑分散，无 Repository 抽象，难以热切底座
- trace 与 memory 混表，清理互相干扰

本方案综合三套候选架构（pgvector 6 步流水线 / conversations-messages-digest / 渐进式 SQLite）的优点，给出最终落地方案。

---

## 一、设计原则

1. **本地零依赖** — 保留 SQLite + sqlite-vec 底座，不引入 PG/Qdrant；规模化通过 Repository 抽象切换
2. **5 种记忆类型同构存储** — 一张 `memory_entries` 表 + `memory_type` 区分，避免表数膨胀
3. **DB 内完成混合检索** — 单 SQL 完成 FTS5 + vec + 信号加权融合,少一次进程切换
4. **异步与同步严格分离** — 主路径只做 dedup + INSERT；embedding/relations/LLM 评估全异步
5. **演进而非重写** — 在现有 `memory.db` 上加列加表，迁移脚本可灰度，343 测试可演进

---

## 二、记忆模型（5 种类型）

| 类型 | 存储 | 生命周期 | 来源 | 用途 |
|---|---|---|---|---|
| **Core** | `core_memory` 独立表 | 永久（可编辑） | LLM 抽取 + 用户/Agent 显式声明 | 始终注入 system prompt 前缀（≤4KB） |
| **Working** | 内存 LRU（SessionRegistry） | 会话结束销毁 | 当前 session 最近 N 轮 | 多轮指代/上下文 |
| **Episodic** | `memory_entries` + `conversations` | 30–90 天 → 蒸馏 | Agent 输出、用户消息 | 对话/任务事件 |
| **Semantic** | `memory_entries` (type=semantic) | 永久 | 原子事实提取 + 蒸馏 worker | 稳定事实/知识 |
| **Procedural** | `procedural_signals` 独立表 | 滚动统计 | 路由决策 + 用户行为 | Agent 偏好/路由信号 |

**辅助结构**：`conversations`（episodic 归集）、`memory_relations`（实体关系图）。

---

## 三、数据库 Schema

### memory.db v2

```sql
-- 主表：5 种类型同构存储
CREATE TABLE memory_entries (
  memory_id           TEXT PRIMARY KEY,
  user_id             TEXT NOT NULL,
  memory_type         TEXT NOT NULL
                      CHECK(memory_type IN ('episodic','semantic','procedural')),
  conversation_id     TEXT,                          -- 仅 episodic
  content             TEXT NOT NULL,
  summary             TEXT NOT NULL,
  source_agent        TEXT,
  source_task_id      TEXT,
  tags                TEXT DEFAULT '[]',             -- JSON
  importance          REAL DEFAULT 0.5,
  content_hash        TEXT NOT NULL,                 -- simhash，dedup
  access_count        INTEGER DEFAULT 0,
  embedding_model     TEXT,
  embedding_version   INTEGER DEFAULT 1,
  parent_id           TEXT,                          -- chunking 父
  distilled           INTEGER DEFAULT 0,
  expire_at           TEXT,
  created_at          TEXT NOT NULL,
  last_accessed_at    TEXT,
  version             INTEGER DEFAULT 1,
  access_level        TEXT DEFAULT 'shared',
  FOREIGN KEY (conversation_id) REFERENCES conversations(conversation_id)
);

CREATE INDEX idx_mem_user_type      ON memory_entries(user_id, memory_type, created_at DESC);
CREATE INDEX idx_mem_importance     ON memory_entries(importance DESC, last_accessed_at DESC);
CREATE INDEX idx_mem_hash           ON memory_entries(content_hash);
CREATE INDEX idx_mem_conv           ON memory_entries(conversation_id);
CREATE INDEX idx_mem_expire         ON memory_entries(expire_at) WHERE expire_at IS NOT NULL;

-- 对话容器（episodic 归集）
CREATE TABLE conversations (
  conversation_id     TEXT PRIMARY KEY,
  user_id             TEXT NOT NULL,
  topic               TEXT,
  summary             TEXT,
  participant_agents  TEXT DEFAULT '[]',             -- JSON
  message_count       INTEGER DEFAULT 0,
  importance          REAL DEFAULT 0.5,
  first_message_at    TEXT NOT NULL,
  last_message_at     TEXT NOT NULL,
  status              TEXT DEFAULT 'active',         -- active / distilled / archived
  topic_embedding     BLOB
);
CREATE INDEX idx_conv_user_last     ON conversations(user_id, last_message_at DESC);
CREATE INDEX idx_conv_status        ON conversations(status, last_message_at DESC);

-- Core Memory（始终注入）
CREATE TABLE core_memory (
  user_id     TEXT NOT NULL,
  slot_key    TEXT NOT NULL,                         -- profile / current_projects / preferences / pinned_facts
  slot_value  TEXT NOT NULL,                         -- JSON
  byte_size   INTEGER NOT NULL,
  updated_at  TEXT NOT NULL,
  updated_by  TEXT,                                  -- agent_id / 'user' / 'distillation'
  PRIMARY KEY (user_id, slot_key)
);

-- 实体关系图
CREATE TABLE memory_relations (
  relation_id       INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id           TEXT NOT NULL,
  head_entity       TEXT NOT NULL,
  relation          TEXT NOT NULL,
  tail_entity       TEXT NOT NULL,
  head_memory_id    TEXT,
  tail_memory_id    TEXT,
  confidence        REAL DEFAULT 0.8,
  ts                TEXT NOT NULL,                   -- 关系成立时间
  created_at        TEXT NOT NULL
);
CREATE INDEX idx_rel_head     ON memory_relations(user_id, head_entity);
CREATE INDEX idx_rel_tail     ON memory_relations(user_id, tail_entity);
CREATE INDEX idx_rel_relation ON memory_relations(relation);

-- Procedural 信号（路由偏好/成功率）
CREATE TABLE procedural_signals (
  user_id       TEXT NOT NULL,
  signal_key    TEXT NOT NULL,                       -- 例: route_pref:code / agent_success:claude_code
  signal_value  TEXT NOT NULL,
  weight        REAL DEFAULT 1.0,
  hits          INTEGER DEFAULT 0,
  updated_at    TEXT NOT NULL,
  PRIMARY KEY (user_id, signal_key)
);

-- FTS5（jieba 分词器，写时索引 content）
CREATE VIRTUAL TABLE memory_fts USING fts5(
  memory_id UNINDEXED, content, summary, tags,
  tokenize='jieba'
);

-- 向量索引（多版本共存）
CREATE VIRTUAL TABLE vec_memory_v1 USING vec0(memory_id TEXT PRIMARY KEY, embedding FLOAT[384]);
-- 切大模型时新建 vec_memory_v2 FLOAT[1024]，迁移期双写
```

### trace.db（从 memory.db 拆出）

```sql
CREATE TABLE routing_traces (
  trace_id          TEXT PRIMARY KEY,
  user_id           TEXT,
  raw_message       TEXT,
  decision_payload  TEXT,                            -- JSON
  created_at        TEXT NOT NULL
);
CREATE INDEX idx_trace_created ON routing_traces(created_at DESC);
CREATE INDEX idx_trace_user    ON routing_traces(user_id, created_at DESC);
```

### archive.db（按月分表）

```sql
CREATE TABLE archive_2026_05 (
  memory_id           TEXT PRIMARY KEY,
  user_id             TEXT,
  memory_type         TEXT,
  content_compressed  BLOB,                          -- gzip
  summary             TEXT,
  importance          REAL,
  original_created_at TEXT,
  archived_at         TEXT NOT NULL
);
-- 每月新建一张，运维易于按月清理/外迁
```

---

## 四、写入流水线（10 步）

```
Agent 输出 / 用户消息
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│ MemoryService.write(entry, ctx)                           │
├──────────────────────────────────────────────────────────┤
│ 1. 类型识别       → episodic / semantic / procedural      │
│ 2. content_hash   → simhash; dedup 命中则 access_count++  │
│ 3. importance     → 规则信号 + 可选 LLM 短评 (0-1)         │
│ 4. 归属 conv      → 同 user + 距上条 <2h + 主题相似        │
│    (仅 episodic)    LLM 判定窄候选 1-3，不命中则新建        │
│ 5. Chunking       → content > 500 字按句切分，parent_id    │
│ 6. INSERT (同步)  → memory_entries + memory_fts            │
│    └─ 携 embedding_model + embedding_version              │
│ 7. 异步: 生成 embedding → 写 vec_memory_v{N}              │
│ 8. 异步: NER+RE 抽取 → 写 memory_relations                 │
│ 9. 异步: Core Memory 候选检测 → 待确认队列                  │
│ 10. 容量检查      → 软上限触发蒸馏，硬上限按加权 LRU         │
└──────────────────────────────────────────────────────────┘
```

**importance 评分（规则）**：
```
score = 0.3 * has_user_emphasis    # 用户用了"记住/重要/务必"等
      + 0.2 * is_fact              # type=fact
      + 0.2 * length_score         # 短消息低分
      + 0.2 * has_entity           # 含命名实体
      + 0.1 * source_credibility   # agent credibility
```

**Core Memory 候选检测**：
- 触发词："我是 / 我喜欢 / 我在做 / 我的项目"
- 进入 `core_memory_candidates` 队列（隐性表）
- 用户/管理员在面板"确认更新"

---

## 五、检索流水线（7 步）

```
RouteRequest
        │
        ▼
┌──────────────────────────────────────────────────────────┐
│ MemoryService.retrieve(message, ctx)                      │
├──────────────────────────────────────────────────────────┤
│ 1. Core Memory     → SELECT * FROM core_memory             │
│    (固定注入)        拼接 system prompt 前缀 (≤4KB)         │
│                                                            │
│ 2. Working Memory  → SessionRegistry 取最近 N 轮            │
│                                                            │
│ 3. Query Understand → Core LLM 改写                        │
│    • 时间消歧 "上次/上周/那天" → 具体日期范围               │
│    • 指代消解 "那个 X" → 实体                              │
│    • 输出: normalized_query, entities[], time_range        │
│                                                            │
│ 4. Multi-Route Recall (DB 内单 SQL)                        │
│    ├─ Pre-filter: user_id + memory_type + time_range       │
│    ├─ FTS5 MATCH (jieba)           → 候选 30               │
│    ├─ vec_distance_cosine          → 候选 30               │
│    ├─ memory_relations 1-hop       → 候选 10               │
│    └─ DB 内 UNION + 信号加权 ORDER BY:                     │
│       score = 0.5·rrf                                      │
│             + 0.2·recency_score                            │
│             + 0.2·importance                               │
│             + 0.1·log(access_count+1)                      │
│       → top 30                                             │
│                                                            │
│ 5. Reranker (可选开关)                                     │
│    bge-reranker-v2-m3 对 top 30 重排 → top 10              │
│                                                            │
│ 6. Post-filter                                             │
│    • content_hash 同的合并                                  │
│    • access_level 校验                                     │
│    • 长度截断                                              │
│                                                            │
│ 7. Context Assembler                                       │
│    [Core 固定] + [Working] + [Recall by conversation]      │
│    总长 ≤ 8KB (可配)                                       │
└──────────────────────────────────────────────────────────┘
```

**多路召回的 DB 内 SQL**（示意）：
```sql
WITH fts AS (
  SELECT memory_id, rank, ROW_NUMBER() OVER (ORDER BY rank) AS rk
  FROM memory_fts WHERE memory_fts MATCH ? LIMIT 30
),
vec AS (
  SELECT memory_id, vec_distance_cosine(embedding, ?) AS dist,
         ROW_NUMBER() OVER (ORDER BY dist) AS rk
  FROM vec_memory_v1 LIMIT 30
),
graph AS (
  SELECT head_memory_id AS memory_id, 0 AS rk
  FROM memory_relations WHERE head_entity IN (?, ?, ...) LIMIT 10
),
candidates AS (
  SELECT memory_id, 1.0/(60+rk) AS rrf FROM fts
  UNION ALL
  SELECT memory_id, 1.0/(60+rk) AS rrf FROM vec
  UNION ALL
  SELECT memory_id, 1.0/(60+rk) AS rrf FROM graph
)
SELECT m.*,
       SUM(c.rrf) AS rrf_score,
       (julianday('now') - julianday(m.last_accessed_at)) AS age_days,
       (0.5*SUM(c.rrf)
        + 0.2*EXP(-0.05*age_days)
        + 0.2*m.importance
        + 0.1*LN(m.access_count+1)) AS final_score
FROM candidates c JOIN memory_entries m USING(memory_id)
WHERE m.user_id = ? AND m.memory_type IN (?)
GROUP BY m.memory_id
ORDER BY final_score DESC LIMIT 30;
```

---

## 六、后台 Workers（4 个）

| Worker | 频率 | 任务 |
|---|---|---|
| **DistillationWorker** | 每日 02:00 | 取 7-30 天前 importance<0.4 的 episodic，按 conv 聚合 → LLM 总结 → 写 semantic，原条标 distilled=1 |
| **ArchivalWorker** | 每周日 03:00 | 取 90 天前 distilled=1 的记忆 → gzip 压缩 → 迁移 archive_YYYY_MM，主库删除 |
| **ImportanceRecomputeWorker** | 每日 04:00 | importance *= 0.99（时间衰减）+ access_count 反向加权 |
| **EmbeddingMigrationWorker** | 按需触发 | 切 embedding 模型时，批量重建 vec_memory_v{N+1}，双版本期间检索 fallback |

调度方案：**APScheduler**（不引入 Celery/Airflow，保持本地工具定位）。

---

## 七、代码结构（Repository 抽象）

```
src/agentmind/memory/
├── __init__.py
├── types.py                       # MemoryEntry / Conversation / CoreMemory / Relation / Signal
├── repository.py                  # IMemoryStore 接口
├── sqlite_store.py                # 当前实现（默认）
├── service.py                     # MemoryService 编排
│
├── pipeline/
│   ├── write_pipeline.py          # 10 步写入
│   ├── query_understanding.py     # Core LLM 改写
│   ├── recall.py                  # DB 内单 SQL 多路召回
│   ├── reranker.py                # bge-reranker 封装
│   └── assembler.py               # Context 组装
│
├── components/
│   ├── importance_scorer.py
│   ├── content_hasher.py          # simhash
│   ├── conversation_merger.py
│   ├── core_memory_manager.py
│   ├── relation_extractor.py      # 异步 NER+RE
│   └── chunker.py
│
├── workers/
│   ├── distillation.py
│   ├── archival.py
│   ├── importance_recompute.py
│   ├── embedding_migration.py
│   └── scheduler.py               # APScheduler 入口
│
├── tokenizer/
│   └── jieba_fts.py               # FTS5 jieba 注册
│
└── migrations/
    ├── 001_v1_to_v2.sql           # 加列 + 新表
    ├── 002_trace_split.sql        # trace 拆库
    └── 003_archive_init.sql
```

**对外接口**通过 `MemoryService` 单例，旧 `write_memory/search_memory` 重定向（保留兼容期 1 个版本）。

---

## 八、配置项（settings.yaml）

```yaml
memory:
  # 容量
  max_entries: 200000
  soft_capacity: 160000

  # TTL
  episodic_ttl_days: 90
  semantic_ttl_days: 0              # 0 = 永久
  procedural_ttl_days: 365

  # 写入
  dedup_enabled: true
  importance_llm_eval: false
  chunking_threshold: 500

  # Conversation 归并
  conversation_window_hours: 2
  conversation_sim_threshold: 0.7
  conversation_judge_llm: true

  # Core Memory
  core_memory_max_bytes: 4096
  core_memory_slots: [profile, current_projects, preferences, pinned_facts]
  core_memory_auto_extract: true
  core_memory_user_confirm: true    # 候选需用户/管理员确认

  # 检索
  recall_top_k: 30
  context_top_k: 10
  reranker_enabled: false
  reranker_model: bge-reranker-v2-m3
  context_max_bytes: 8192

  signal_weights:
    rrf: 0.5
    recency: 0.2
    importance: 0.2
    access_count: 0.1

  # 关系图
  relations_enabled: true
  relations_extract_async: true

  # Worker
  workers:
    distillation_cron: "0 2 * * *"
    archival_cron: "0 3 * * 0"
    importance_recompute_cron: "0 4 * * *"

embedding:
  primary:
    model: text-embedding-3-small
    dimension: 384
  fallback:
    model: all-MiniLM-L6-v2
    dimension: 384
  migration:
    enabled: false
    batch_size: 100
    interval_seconds: 60
```

---

## 九、实施路线（5 个里程碑，总工期 ~9 周）

| 里程碑 | 时长 | 内容 | 验收 |
|---|---|---|---|
| **M1: 数据模型 + Repository 抽象** | 2 周 | 新建 `memory/` 包；定义 IMemoryStore；写 migration 脚本（加列+新表+trace 拆库）；现有 `write_memory/search_memory` 重定向到 MemoryService | 343 测试全过 + schema 测试 |
| **M2: 写入侧改造** | 2 周 | content_hash dedup / importance 评分 / conversation 归并 / chunking / 异步队列（embedding/relations/core 候选） | 写入吞吐 ≥ 10 ops/s，dedup 命中率 > 20% |
| **M3: 检索侧改造** | 2 周 | jieba FTS5 tokenizer / DB 内单 SQL 多路召回 / Query Understanding / Context Assembler | 检索 P50 < 100ms,召回@10 > 80% |
| **M4: 关系图 + Reranker** | 1.5 周 | NER+RE 异步抽取 / memory_relations 1-hop 扩展 / bge-reranker 接入 | 关系图测试 + 重排精度测试 |
| **M5: Workers + 运维** | 1.5 周 | 4 个 worker / archive.db 月份分表 / 面板新增（Core 编辑、关系图、worker 状态、容量告警） | 模拟 10 万条 + 30 天周期跑通 |

每个里程碑都向后兼容（旧 API 保留），可单独发版。

---

## 十、关键技术决策

| 决策 | 选择 | 原因 |
|---|---|---|
| 存储底座 | SQLite + sqlite-vec | 保持本地零依赖；Repository 抽象留切换口子 |
| 中文分词 | FTS5 注册 jieba tokenizer | 不存 tokens 列冗余 |
| 向量索引 | sqlite-vec 多版本表共存 | 切模型可渐进迁移 |
| trace 存储 | 独立 trace.db | 不污染 memory.db，清理互不干扰 |
| 异步队列 | asyncio Queue + thread pool | 不引入 Celery/RQ |
| 定时调度 | APScheduler | 不引入 Airflow |
| Reranker | 默认关闭 | 启动慢（~300MB），按需启用 |
| Core Memory 更新 | 候选 + 用户确认 | 防止 Agent 污染 |
| 关系抽取 | 异步 + 限频 | 不阻塞主路径 |
| 信号加权 | DB 内 SQL ORDER BY | 单查询完成 |

---

## 十一、关键指标 / 验收

| 指标 | 目标 |
|---|---|
| 写入 P50 / P99 | < 50ms / < 200ms |
| 检索 P50（不含 reranker） | < 100ms |
| 检索 P50（含 reranker） | < 400ms |
| 召回率 @ Top10 | > 80%（基于标注集） |
| Core Memory 命中率 | > 90%（"我是谁/我在做什么"类） |
| 10 万条数据检索 P99 | < 200ms |
| 蒸馏后存储占用降幅 | ≥ 30% |
| 写入吞吐（讨论模式 5 Agent 并发） | ≥ 10 ops/s |
| dedup 命中率 | > 20% |

---

## 十二、功能变化对比

| 场景 | 当前 v3 | v4 最终 |
|---|---|---|
| "我是谁/我在做什么" | 不一定检索到 | Core Memory 始终注入，必然命中 |
| 跨多轮指代 | recency 朴素拼接 | Working Memory + Query 改写 |
| 同一事实重复写入 | 多条 LWW | dedup + access_count |
| 长文本检索 | 单 entry，部分内容召回失败 | chunking + parent 关联 |
| 实体关联查询（"X 同事都干了啥"） | 不支持 | memory_relations 1-hop |
| 路由偏好学习 | 无 | procedural_signals |
| 30 天后老记忆 | 全删 | 蒸馏成 semantic |
| 1 年后记忆量增长 | 容量上限切除老数据 | 冷热分层 + 归档压缩 |
| 切换 embedding 模型 | 全量重建 | 多版本共存渐进迁移 |
| 中文检索精度 | unicode61，"会议"找不到"飞书会议" | jieba 分词，准确命中 |
| trace 查询 | 与记忆混表 | 独立 trace.db |
| 多用户隔离 | user_id 列存在但弱约束 | Repository 强制 user_id |

---

## 十三、风险与缓解

| 风险 | 缓解 |
|---|---|
| jieba 依赖膨胀（~50MB） | 默认安装，sqlite tokenizer 失败降级 unicode61 |
| Reranker 启动慢（~300MB） | 默认关闭，启用时懒加载 |
| LLM 评估成本 | importance_llm_eval / conversation_judge_llm 全部独立开关 |
| 关系抽取错误率 | confidence 字段 + 面板可审计删除 |
| Core Memory 被 Agent 污染 | 候选 + 用户确认机制 |
| 迁移失败 | 每个 migration 可回滚 + 备份原 db 文件 |
| 异步队列堆积 | 队列长度告警 + 容量限制 |
| 343 测试回归 | 旧 API 重定向兼容期 + CI 全过门槛 |

---

## 附录 A：三套候选方案的取舍

| 候选 | 核心创新 | 缺失 | 本方案吸收 |
|---|---|---|---|
| 方案1 (pgvector 6 步) | Core Memory always-in-prompt / memory_relations 图 / DB 内 RRF / 信号加权 | 写入侧、与本地工具定位冲突 | Core Memory、关系图、信号加权、DB 内融合 |
| 方案2 (conversations/messages/digest) | conversation 容器 / 写时分词 / 懒摘要 | Core/importance/dedup/图/记忆维度窄化为对话 | conversation 容器 |
| 方案3 (渐进式 SQLite) | importance + dedup + Repository + 蒸馏 + 版本管理 | 对话粒度、Core | 全部纳入 |

**决策**：底座不换（SQLite），思想全吸收，演进式落地。

---

## 附录 B：术语

- **Core Memory**：始终注入 system prompt 前缀的结构化用户画像
- **Working Memory**：当前 session 的工作上下文（内存 LRU）
- **Episodic Memory**：单次事件/对话的记忆
- **Semantic Memory**：稳定事实/知识
- **Procedural Memory**：路由偏好/Agent 历史成功率等过程信号
- **RRF (Reciprocal Rank Fusion)**：倒数排名融合，多路检索结果合并算法
- **simhash**：内容指纹算法，用于近似去重
- **HNSW**：Hierarchical Navigable Small World，向量索引算法
- **NER/RE**：命名实体识别 / 关系抽取

---

**方案 owner**：待指定
**最后更新**：2026-05-25
