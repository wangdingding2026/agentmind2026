CLAUDE.md
用于减少常见 LLM 编码错误的行为准则。根据需要与项目特定说明合并使用。
权衡： 这些准则倾向于谨慎而非速度。对于简单任务，请自行判断。

1. 编码前先思考
不要想当然。不要掩饰困惑。呈现权衡取舍。

在实现之前：
明确陈述你的假设。如果不确定，就提问。
如果存在多种理解，把它们都列出来——不要默默地擅自选择。
如果存在更简单的方法，说出来。在必要时提出反对意见。
如果有什么不清楚的，停下来。指出令人困惑的地方。提问。

2. 简洁优先
用最少的代码解决问题。不要写投机性的代码。

不要实现超出需求的功能。
不要为只用一次的代码做抽象。
不要添加未被请求的"灵活性"或"可配置性"。
不要为不可能发生的场景做错误处理。
如果你写了 200 行代码，而 50 行就能搞定，那就重写。
问问自己："高级工程师会认为这过于复杂吗？"如果是，就简化它。

3. 精准修改
只动你必须动的。只清理你自己造成的混乱。

编辑现有代码时：
不要"改进"相邻的代码、注释或格式。
不要重构没坏的东西。
遵循现有的风格，即使你自己的做法会不一样。
如果你注意到无关的死代码，提一下——但不要删除它。

当你的修改产生了孤立代码时：
移除因你的修改而变得未使用的导入/变量/函数。
除非被要求，否则不要移除原先就存在的死代码。
检验标准：每一行修改都应该能直接追溯到用户的请求。

4. 目标驱动执行
定义成功标准。循环直至验证通过。

将任务转化为可验证的目标：
“添加验证” → “为无效输入编写测试，然后让它们通过”
“修复 bug” → “编写一个能重现它的测试，然后让它通过”
“重构 X” → “确保重构前后的测试都能通过”

对于多步骤任务，陈述一个简要计划：
[步骤] → 验证: [检查方式]
[步骤] → 验证: [检查方式]
[步骤] → 验证: [检查方式]
强有力的成功标准让你可以独立循环。软弱的标准（“让它能跑起来”）则需要不断澄清。

这些准则奏效的标志是： diff 中不必要的修改更少，因过度复杂导致的重写更少，澄清问题发生在实现之前而不是犯错之后。

5. 每次会话开始时，自动调用 /using-superpowers 技能